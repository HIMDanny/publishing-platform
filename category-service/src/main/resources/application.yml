server:
  port: 8087

spring:
  application:
    name: category
  datasource:
    url: jdbc:postgresql://localhost:5432/category
    username: mike
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true